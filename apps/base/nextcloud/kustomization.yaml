apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - database.yaml
  #- service.yaml # chart creates its own service, so this is not needed
  - namespace.yaml
  - repository.yaml
  - release.yaml
  - storage.yaml
  